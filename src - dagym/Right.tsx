import { CSSProperties } from "react";

const right = () => {
  const divStyle: CSSProperties = {
    width: "100%",
    display: "flex",
    flexDirection: "column",
  };
  return <div></div>;
};

export default right;
